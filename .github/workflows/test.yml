name: Basic Test

on:
  push:
    branches: [ main, develop ]
  pull_request:
    types: [ opened, synchronize, reopened ]

jobs:
  test:
    name: Basic Test
    runs-on: ubuntu-latest
    steps:
      - name: Simple Check
        run: |
          echo "Simple test passed"
          exit 0 