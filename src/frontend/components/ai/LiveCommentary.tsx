import React from 'react';

export const LiveCommentary: React.FC = () => (
  <div role="region" aria-label="LiveCommentary placeholder" style={{padding:16, border:'1px dashed #ccc'}}>
    Live Commentary (placeholder)
  </div>
);

export default LiveCommentary;
