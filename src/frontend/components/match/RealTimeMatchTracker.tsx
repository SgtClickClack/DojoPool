import React from 'react';

const RealTimeMatchTracker: React.FC = () => {
  return (
    <div
      role="region"
      aria-label="RealTimeMatchTracker placeholder"
      style={{ padding: 16, border: '1px dashed #ccc' }}
    >
      Real-Time Match Tracker (placeholder)
    </div>
  );
};

export default RealTimeMatchTracker;
