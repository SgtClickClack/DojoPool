"""User authentication models."""

from datetime import datetime
from typing import Optional

from ...models.user import User  # Import the main User model

# The User model is now imported from models.user
# This file is kept for backwards compatibility and potential future auth-specific models
