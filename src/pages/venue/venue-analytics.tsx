import React from 'react';
import Layout from '../../components/layout/Layout';
import VenueAnalytics from '../../components/analytics/VenueAnalytics';

const VenueAnalyticsPage: React.FC = () => {
  return (
    <Layout>
      <VenueAnalytics />
    </Layout>
  );
};

export default VenueAnalyticsPage; 