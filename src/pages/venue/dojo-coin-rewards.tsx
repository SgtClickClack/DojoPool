import React from 'react';
import Layout from '../../components/layout/Layout';
import DojoCoinRewards from '../../components/venue/DojoCoinRewards';

const DojoCoinRewardsPage: React.FC = () => {
  return (
    <Layout>
      <DojoCoinRewards />
    </Layout>
  );
};

export default DojoCoinRewardsPage; 