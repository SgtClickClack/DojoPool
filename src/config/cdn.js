// This is a stub for the CDN configuration.
// Replace the contents with your actual CDN settings if available.

const cdnConfig = {
  // Example configuration (update as necessary):
  basePath: "",
  assetPrefix: "",
};

module.exports = cdnConfig; 