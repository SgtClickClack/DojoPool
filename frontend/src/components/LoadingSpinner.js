import React from 'react';

function LoadingSpinner() {
    return (
        <div className="spinner">
            {/* An SVG or CSS spinner implementation */}
            Loading...
        </div>
    );
}

export default LoadingSpinner; 