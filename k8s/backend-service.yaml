apiVersion: v1
kind: Service
metadata:
  name: dojopool-backend-service
  namespace: dojopool
  labels:
    app: dojopool-backend
spec:
  selector:
    app: dojopool-backend
  ports:
    - name: http
      port: 3002
      targetPort: 3002
      protocol: TCP
  type: ClusterIP
