import React from 'react';

// Minimal placeholder implementation for WorldHub to unblock imports during refactor.
// If a richer MapView exists, we can integrate it here later.
const WorldHub: React.FC = () => {
  return (
    <div style={{ padding: 16 }}>
      <h2>World Hub</h2>
      <p>This is a placeholder WorldHub component under apps/web/src.</p>
      <p>Replace with the full interactive map once migration completes.</p>
    </div>
  );
};

export default WorldHub;
