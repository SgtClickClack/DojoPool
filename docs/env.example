# DojoPool environment example
# Copy this file to `.env` (for root), `.flaskenv` (for Flask), and/or set per your process manager.
# Do not commit real secrets.

# Node/Next/Vite
NODE_ENV=development
PORT=3000
# Used by Next.js rewrites in next.config.js
API_BASE_URL=http://localhost:3002
# Vite dev proxy expects backend on 8080 in dev per guidelines
DEV_API_URL=http://localhost:8080

# Flask backend
FLASK_APP=dojopool
FLASK_ENV=development
FLASK_DEBUG=1
PYTHONUNBUFFERED=1
# Flask binds behind Nginx/Gunicorn in production; 5000 for local
FLASK_RUN_PORT=5000

# Security
JWT_SECRET=replace_me
SESSION_SECRET=replace_me
CSRF_SECRET=replace_me
CORS_ORIGIN=http://localhost:3000

# Database
MONGO_URI=mongodb://localhost:27017/dojopool
REDIS_URL=redis://localhost:6379

# OpenAI/AI services
OPENAI_API_KEY=
TFJS_BACKEND=webgl

# WebSockets
WS_URL=ws://localhost:8080

# Analytics (restrict in prod CSP)
ANALYTICS_WRITE_KEY=

# Images/Assets
NEXT_PUBLIC_IMAGE_DOMAINS=localhost,dojopool.com.au

# Docker/Runtime
GUNICORN_WORKERS=2
NGINX_WORKER_PROCESSES=1
SUPERVISOR_LOG_LEVEL=info
